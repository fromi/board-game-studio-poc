{
  "name": "root",
  "private": true,
  "type": "module",
  "devDependencies": {
    "lerna": "^3.16.4"
  },
  "dependencies": {
    "@bga/not-alone": "./packages/not-alone",
    "express": "latest",
    "mongoose": "^5.7.1"
  },
  "scripts": {
    "start": "node index.js",
    "heroku-postbuild": "cd packages/not-alone-ui && npm install && npm run build"
  },
  "engines": {
    "node": "12.x"
  }
}
